# Survivants d'Acres Joomla Extension
A Collection of Joomla extensions for Managing Event participations as Reenactment Group.

# How to use on Unix:

Prerequisites:
- WSL2 (Unix)
- GIT

After cloning the repository check the config/.env file and run "sudo ./build_dev_env.sh"